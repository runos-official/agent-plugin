{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "runos": {
      "type": "stdio",
      "command": "./bin/runos-mcp",
      "args": [
        "mcp",
        "serve",
        "read"
      ],
      "cwd": "${PLUGIN_ROOT}"
    }
  }
}
